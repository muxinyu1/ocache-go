syntax = "proto3";

option go_package = "./impl";

package ocachepb;

message GetRequest {
  string group = 1;
  string key = 2;
}

message GetResponse {
  bytes value = 1;
}

service GroupCache {
  rpc Get(GetRequest) returns (GetResponse);
}
